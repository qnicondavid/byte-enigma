/**
 * Draws the diagrams the documentation shows.
 *
 * <p>Every figure here is generated from something the repository already commits: a sweep
 * checkpoint, or the cipher itself. Nothing is transcribed by hand, because a number copied into a
 * drawing drifts from its source the same way a number copied into a sentence does, only more
 * quietly, since nobody rereads an SVG.
 *
 * <p>Not part of the published library. This module exists so the pictures cannot lie.
 */
package io.github.qnicondavid.byteenigma.tools;
