/**
 * The command line.
 *
 * <p>Six subcommands, no dependencies, forty lines of argument parsing. The flags are meant to teach
 * the same thing the library does: {@code seal} takes a nonce and {@code raw} does not, and
 * {@code raw} says on stderr what that costs you.
 */
package io.github.qnicondavid.byteenigma.cli;
